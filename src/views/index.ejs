<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <title>URL Shortener</title>
</head>
<body>
    <div class="container">

            <h1 class="text-center mt-5">URL Shortener</h1>

            <form action="/shorten" method="post" class="d-flex justify-content-center mt-5">
                <div class="row g-3 mb-3">
                    <div class="col-auto">
                        <input type="text" name="baseUrl" id="baseUrl" class="form-control">
                    </div>
                    <div class="col-auto">
                        <button type="submit" class="btn btn-primary">Shorten</button>
                    </div>               
                </div>           
            </form>
        

        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Full URL</th>
                    <th scope="col">Short URL</th>
                </tr>
            </thead>
            <tbody>
                <% newUrl.forEach(urls => { %>
                    <tr>
                        <td><a href="<%= urls.newUrl %>"><%= urls.baseUrl %></a></td>
                        <td><a href="<%= urls.newUrl %>"><%= urls.newUrl %></a></td>
                    </tr>                
                <% }) %>
            </tbody>
        </table>
    </div>   
    
</body>
</html>